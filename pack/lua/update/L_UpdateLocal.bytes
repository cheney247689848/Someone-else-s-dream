--更新本地资源
require "udpate/L_UpdateConfig"

--定义模块
L_UpdateLocal = {}
setmetatable(L_UpdateLocal, {__index = _G})
local _ENV = L_UpdateLocal

Process_downloadbundle = {}
--更新网上资源
function Process_loadlocalview:Enter()

    print("--更新网上资源--")
    --与本地信息匹配后更新信息
    --本地信息用parfab key保存
end

function Process_loadlocalview:Execute()

    print("--Execute--")
    --更新进度
    --更新后转入登陆场景
end

function Process_loadlocalview:Exit()

    
end